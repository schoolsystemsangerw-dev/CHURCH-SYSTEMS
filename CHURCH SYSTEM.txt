<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Citizen Request & Resolution Portal | Matrimony</title>
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Supabase JS Client -->
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
</head>
<body class="bg-slate-50 text-slate-800 min-h-screen flex flex-col">

  <!-- Header with Logos -->
  <header class="bg-indigo-950 text-white shadow-lg border-b-4 border-indigo-700">
    <div class="max-w-xl mx-auto p-4 flex items-center justify-between">
      
      <!-- Diocese Logo (Left) -->
      <div class="flex items-center">
        <img src="log-diocese.png" alt="Diocese Logo" class="h-16 w-auto object-contain" />
      </div>

      <!-- Main Portal Title -->
      <div class="text-center px-2">
        <h1 class="font-bold text-base sm:text-lg tracking-wide uppercase leading-tight">Citizen Request & Resolution Portal</h1>
        <p class="text-xs text-indigo-200 mt-1">Matrimony System</p>
      </div>

      <!-- EAR Rwanda Logo (Right) -->
      <div class="flex items-center">
        <img src="logo-rwanda.png" alt="EAR Rwanda Logo" class="h-16 w-auto object-contain" />
      </div>
      
    </div>
  </header>

  <!-- Role Badge -->
  <div class="bg-indigo-100 border-b border-indigo-200 py-2 px-4">
    <div class="max-w-xl mx-auto flex justify-end">
      <span class="text-xs bg-indigo-800 text-white font-semibold px-3 py-1 rounded-full uppercase tracking-wider">Umukristo Portal</span>
    </div>
  </div>

  <!-- Main Content Container -->
  <main class="flex-grow p-4 max-w-xl mx-auto w-full">

    <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
      <h2 class="text-xl font-bold text-slate-900 mb-1">Matrimony Certificate Request</h2>
      <p class="text-sm text-slate-500 mb-6">Uzusaza amakuru yombi neza maze wohereze icyifuzo cyawe.</p>

      <form id="matrimonyForm" class="space-y-5">

        <!-- Parish Selection -->
        <div>
          <label class="block text-xs font-semibold text-slate-700 uppercase mb-1">Select Parish (Paruwasi)</label>
          <select id="parishSelect" required class="w-full p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none bg-white">
            <option value="">-- Hitamo Paruwasi --</option>
            <!-- Dynamically loaded from Supabase -->
          </select>
        </div>

        <hr class="border-slate-100" />

        <!-- Groom Details -->
        <div class="space-y-3">
          <h3 class="text-sm font-semibold text-indigo-900">1. Groom Information (Umukwe)</h3>
          <div>
            <label class="block text-xs text-slate-600 mb-1">Full Name</label>
            <input type="text" id="groomName" required placeholder="Amatafari yose" class="w-full p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none" />
          </div>
          <div>
            <label class="block text-xs text-slate-600 mb-1">National ID Number (16 Digits)</label>
            <input type="text" id="groomId" required maxlength="16" placeholder="119XXXXXXXXXXXXX" class="w-full p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none" />
          </div>
        </div>

        <hr class="border-slate-100" />

        <!-- Bride Details -->
        <div class="space-y-3">
          <h3 class="text-sm font-semibold text-indigo-900">2. Bride Information (Umugeni)</h3>
          <div>
            <label class="block text-xs text-slate-600 mb-1">Full Name</label>
            <input type="text" id="brideName" required placeholder="Amatafari yose" class="w-full p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none" />
          </div>
          <div>
            <label class="block text-xs text-slate-600 mb-1">National ID Number (16 Digits)</label>
            <input type="text" id="brideId" required maxlength="16" placeholder="119XXXXXXXXXXXXX" class="w-full p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none" />
          </div>
        </div>

        <hr class="border-slate-100" />

        <!-- Phone Number -->
        <div>
          <label class="block text-xs text-slate-600 mb-1">Applicant Phone Number</label>
          <input type="tel" id="applicantPhone" required placeholder="078XXXXXXX" class="w-full p-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none" />
        </div>

        <!-- Submit Button -->
        <button type="submit" id="submitBtn" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-medium p-3.5 rounded-lg transition-colors shadow">
          Ohereza Icyifuzo (Submit Request)
        </button>

      </form>
    </div>

  </main>

  <!-- Footer -->
  <footer class="bg-white border-t border-slate-200 p-4 text-center text-xs text-slate-600 font-medium">
    <p>hindura imibereho yawe uyumunsi</p>
  </footer>

  <script type="module">
    import imageCompression from 'https://cdn.jsdelivr.net/npm/browser-image-compression@2.0.2/dist/browser-image-compression.mjs';

    // SUPABASE CONFIGURATION
    // Replace these placeholders with your actual Supabase URL and Anon Key:
    const SUPABASE_URL = 'YOUR_SUPABASE_URL_HERE';
    const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY_HERE';

    const supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

    // Load Parishes into dropdown on page load
    async function loadParishes() {
      const { data, error } = await supabase.from('parishes').select('id, name');
      const select = document.getElementById('parishSelect');
      if (data) {
        data.forEach(p => {
          const opt = document.createElement('option');
          opt.value = p.id;
          opt.textContent = p.name;
          select.appendChild(opt);
        });
      }
    }
    loadParishes();

    // Form Submission Handler
    document.getElementById('matrimonyForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      const btn = document.getElementById('submitBtn');
      btn.disabled = true;
      btn.textContent = "Iri koherezwa...";

      const payload = {
        parish_id: document.getElementById('parishSelect').value,
        groom_name: document.getElementById('groomName').value,
        groom_id_number: document.getElementById('groomId').value,
        bride_name: document.getElementById('brideName').value,
        bride_id_number: document.getElementById('brideId').value,
        phone_number: document.getElementById('applicantPhone').value,
        status: 'pending'
      };

      const { data, error } = await supabase.from('matrimony_requests').insert([payload]);

      if (error) {
        alert("Ikosa ryabaye: " + error.message);
        btn.disabled = false;
        btn.textContent = "Ohereza Icyifuzo (Submit Request)";
      } else {
        alert("Icyifuzo cyawe cyoherejwe neza!");
        document.getElementById('matrimonyForm').reset();
        btn.disabled = false;
        btn.textContent = "Ohereza Icyifuzo (Submit Request)";
      }
    });
  </script>
</body>
</html>